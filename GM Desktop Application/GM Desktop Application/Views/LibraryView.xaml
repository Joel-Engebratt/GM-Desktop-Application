<UserControl x:Class="GM_Desktop_Application.Views.LibraryView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <Grid Margin="0,0,0,28">
            <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <StackPanel Margin="0,0,20,0"><TextBlock Text="Your campaigns" Style="{StaticResource Heading}"/><TextBlock Text="Pick up where your story left off." Foreground="{StaticResource MutedBrush}"/></StackPanel>
            <Button Grid.Column="1" Content="+ _New campaign" Command="{Binding NewCommand}" VerticalAlignment="Center"/>
        </Grid>
        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <TextBlock Text="Loading campaigns…" Visibility="{Binding IsBusy, Converter={StaticResource BoolVisibility}}" AutomationProperties.LiveSetting="Polite"/>
            <TextBlock Text="{Binding Error}" Style="{StaticResource Message}"/>
            <ScrollViewer MaxHeight="100" VerticalScrollBarVisibility="Auto"><TextBlock Text="{Binding Warning}" Style="{StaticResource Message}" Foreground="{StaticResource BrassBrush}"/></ScrollViewer>
            <Button Content="_Retry loading" Command="{Binding LoadCommand}" HorizontalAlignment="Left">
                <Button.Style><Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}"><Style.Triggers><MultiDataTrigger><MultiDataTrigger.Conditions><Condition Binding="{Binding Error}" Value="{x:Null}"/><Condition Binding="{Binding Warning}" Value="{x:Null}"/></MultiDataTrigger.Conditions><Setter Property="Visibility" Value="Collapsed"/></MultiDataTrigger></Style.Triggers></Style></Button.Style>
            </Button>
        </StackPanel>
        <Grid Grid.Row="2">
            <DataGrid x:Name="CampaignTable" ItemsSource="{Binding Campaigns}" SelectedItem="{Binding SelectedCampaign, Mode=TwoWay}"
                      AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single" SelectionUnit="FullRow"
                      CanUserAddRows="False" CanUserDeleteRows="False" CanUserReorderColumns="False" CanUserResizeColumns="False" CanUserResizeRows="False"
                      HeadersVisibility="Column" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="{StaticResource LineBrush}"
                      Background="{StaticResource SurfaceBrush}" Foreground="{StaticResource TextBrush}" BorderThickness="0" RowHeight="48"
                      Sorting="OnSorting" Loaded="OnLoaded" MouseDoubleClick="OnDoubleClick" PreviewKeyDown="OnKeyDown" AutomationProperties.Name="Campaigns">
                <DataGrid.Style><Style TargetType="DataGrid"><Style.Triggers><DataTrigger Binding="{Binding IsBusy}" Value="True"><Setter Property="IsEnabled" Value="False"/></DataTrigger><DataTrigger Binding="{Binding IsEmpty}" Value="True"><Setter Property="Visibility" Value="Collapsed"/></DataTrigger></Style.Triggers></Style></DataGrid.Style>
                <DataGrid.Resources><Style x:Key="CampaignCellText" TargetType="TextBlock"><Setter Property="TextTrimming" Value="CharacterEllipsis"/><Setter Property="TextWrapping" Value="NoWrap"/><Setter Property="ToolTip" Value="{Binding Text, RelativeSource={RelativeSource Self}}"/></Style>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Template"><Setter.Value>
                            <ControlTemplate TargetType="DataGridColumnHeader">
                                <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
                                    <StackPanel Orientation="Horizontal"><ContentPresenter/><TextBlock x:Name="Direction" Margin="6,0,0,0" Foreground="{StaticResource BrassBrush}" Visibility="Collapsed"/></StackPanel>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="SortDirection" Value="Ascending"><Setter TargetName="Direction" Property="Text" Value="↑"/><Setter TargetName="Direction" Property="Visibility" Value="Visible"/></Trigger>
                                    <Trigger Property="SortDirection" Value="Descending"><Setter TargetName="Direction" Property="Text" Value="↓"/><Setter TargetName="Direction" Property="Visibility" Value="Visible"/></Trigger>
                                    <Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="{StaticResource SurfaceBrush}"/></Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value></Setter>
                        <Setter Property="Background" Value="{StaticResource BackgroundBrush}"/><Setter Property="Foreground" Value="{StaticResource MutedBrush}"/>
                        <Setter Property="Padding" Value="16,14"/><Setter Property="BorderThickness" Value="0,0,0,1"/><Setter Property="BorderBrush" Value="{StaticResource LineBrush}"/>
                        <Setter Property="FocusVisualStyle" Value="{StaticResource FocusStyle}"/>
                    </Style>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Background" Value="Transparent"/><Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                        <Setter Property="BorderThickness" Value="0"/><Setter Property="Padding" Value="14,0"/>
                        <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="DataGridCell"><Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}"><ContentPresenter VerticalAlignment="Center"/></Border></ControlTemplate></Setter.Value></Setter>
                        <Setter Property="FocusVisualStyle" Value="{StaticResource FocusStyle}"/>
                        <Style.Triggers><Trigger Property="IsKeyboardFocusWithin" Value="True"><Setter Property="Background" Value="#414039"/></Trigger></Style.Triggers>
                    </Style>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/><Setter Property="BorderBrush" Value="Transparent"/><Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="DataGridRow"><Grid Background="{TemplateBinding Background}"><DataGridCellsPresenter/><Border Background="{TemplateBinding BorderBrush}" Width="3" HorizontalAlignment="Left" IsHitTestVisible="False"/></Grid></ControlTemplate></Setter.Value></Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="#2A2D33"/></Trigger>
                            <Trigger Property="IsSelected" Value="True"><Setter Property="Background" Value="#373329"/><Setter Property="BorderBrush" Value="{StaticResource BrassBrush}"/></Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTextColumn ElementStyle="{StaticResource CampaignCellText}" Header="Name" Binding="{Binding Name}" SortMemberPath="Name" Width="2*" MinWidth="160"/>
                    <DataGridTextColumn ElementStyle="{StaticResource CampaignCellText}" Header="Rules system" Binding="{Binding CustomSystemName}" SortMemberPath="RulesSystem" Width="1.4*" MinWidth="140"/>
                    <DataGridTextColumn ElementStyle="{StaticResource CampaignCellText}" Header="Last updated" Binding="{Binding LastUpdatedDisplay}" SortMemberPath="LastUpdated" SortDirection="Descending" Width="205"/>
                </DataGrid.Columns>
            </DataGrid>
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding IsEmpty, Converter={StaticResource BoolVisibility}}">
                <TextBlock Text="Your first story starts here" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,12"/>
                <TextBlock Text="Create a campaign to start your library." Foreground="{StaticResource MutedBrush}" HorizontalAlignment="Center" Margin="0,0,0,24"/>
                <Button Content="Create campaign" Command="{Binding NewCommand}" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
        <Grid Grid.Row="3" Margin="0,20,0,0">
            <TextBlock Text="{Binding CountDisplay}" Foreground="{StaticResource MutedBrush}" VerticalAlignment="Center"/>
            <Button Content="_Open campaign →" Command="{Binding OpenCommand}" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Right"/>
        </Grid>
    </Grid>
</UserControl>
