<UserControl x:Class="GM_Desktop_Application.Views.CampaignView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <Button Content="← _Back to campaigns" Command="{Binding BackCommand}" HorizontalAlignment="Left" Margin="0,0,0,32"/>
            <TextBlock Text="{Binding Campaign.Name}" Style="{StaticResource Heading}"/>
            <TextBlock Text="Rules system" Foreground="{StaticResource MutedBrush}" Margin="0,20,0,8"/>
            <TextBlock Text="{Binding Campaign.CustomSystemName}" FontSize="18"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
